<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- Video View for videos -->
    <VideoView
        android:id="@+id/carouselVideo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Image View for images and video thumbnails -->
    <ImageView
        android:id="@+id/carouselImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="Product media" />

    <!-- Video play indicator overlay (simple play icon) -->
    <ImageView
        android:id="@+id/videoIndicator"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:src="@android:drawable/ic_media_play"
        android:tint="#FFFFFF"
        android:visibility="gone" />

    <!-- Delete button (X icon) -->
    <ImageView
        android:id="@+id/deleteButton"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="top|end"
        android:layout_margin="8dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:tint="#FFFFFF"
        android:contentDescription="Delete media"
        android:scaleType="centerInside"
        android:padding="6dp"
        android:visibility="gone" />

</FrameLayout>
