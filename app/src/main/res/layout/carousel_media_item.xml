<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mediaContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="true"
    android:clipToPadding="true">

    <!-- Image View -->
    <ImageView
        android:id="@+id/carouselImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:adjustViewBounds="false"
        android:visibility="gone" />

    <!-- TextureView for Video (respects clipping unlike SurfaceView) -->
    <com.example.soukify.ui.shop.ScalableTextureVideoView
        android:id="@+id/carouselVideo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Video Indicator (hidden as per your preference) -->
    <ImageView
        android:id="@+id/videoIndicator"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:src="@android:drawable/ic_media_play"
        android:visibility="gone"
        android:alpha="0.8" />

    <!-- Delete Button (hidden as per your preference) -->
    <ImageButton
        android:id="@+id/deleteButton"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="top|end"
        android:layout_margin="8dp"
        android:background="@drawable/circle_background_white"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:tint="@color/black"
        android:visibility="gone" />

</FrameLayout>